<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE book PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "../../../docbook-xml-4.5/docbookx.dtd">
<chapter id="chapter.searches">
  <title>搜索<indexterm class="singular">
      <primary>搜索</primary>
    </indexterm></title>

  <section id="search.window">
    <title>搜索窗口<indexterm class="singular">
        <primary>快捷键</primary> <secondary>搜索——Ctrl+F</secondary>
      </indexterm></title>

    <para>Open the Search window with <keycombo>
        <keycap>Ctrl</keycap>

        <keycap>F</keycap>
      </keycombo> and enter the word or phrase you wish to search for in the
    <emphasis>Search for</emphasis> box.</para>

    <para>Alternatively, you can select a word or phrase in the Editor pane, Fuzzy matches pane or Glossary pane and hit <keycombo> <keycap>Ctrl</keycap> <keycap>F</keycap> </keycombo>. The word or phrase is entered in the <emphasis>Search for</emphasis> box automatically. 您可以同时打开多个搜索窗口，但别忘记在不需要时关闭它们，这样它们才不会弄乱您的桌面。</para>

    <para>Click the dropdown arrow of the <emphasis>Search for</emphasis> box
    to access the last 10 searches.</para>

    <para>The Search window has its own menus:</para>

    <itemizedlist>
      <listitem>
        <para>File &gt; Search for selection (<keycombo>
            <keycap>Ctrl</keycap>

            <keycap>F</keycap>
          </keycombo>): refocus on the search field and select all its
        contents.</para>
      </listitem>

      <listitem>
        <para>File &gt; Close (<keycombo>
            <keycap>Ctrl</keycap>

            <keycap>W</keycap>
          </keycombo>): close the search window (in the same way as
        <keycap>Esc</keycap>)</para>
      </listitem>

      <listitem>
        <para>Edit &gt; Insert source (<keycombo>
            <keycap>Ctrl</keycap>

            <keycap>Shift</keycap>

            <keycap>I</keycap>
          </keycombo>): insert current segment source.</para>
      </listitem>

      <listitem>
        <para>Edit &gt; Replace with source (<keycombo>
            <keycap>Ctrl</keycap>

            <keycap>Shift</keycap>

            <keycap>R</keycap>
          </keycombo>): replace with current segment source.</para>
      </listitem>

      <listitem>
        <para>Edit &gt; Create Glossary Entry (<keycombo>
            <keycap>Ctrl</keycap>

            <keycap>Shift</keycap>

            <keycap>G</keycap>
          </keycombo>): add a new glossary item.</para>
      </listitem>
    </itemizedlist>
  </section>

  <section id="using.wild.cards">
    <title>使用通配符<indexterm class="singular">
        <primary>搜索</primary> <secondary>使用通配符</secondary>
      </indexterm></title>

    <para>无论是精确还是关键词搜索，都可以使用通配符 '*' 和 '?'。它们的含义Word用户很熟悉：</para>

    <itemizedlist>
      <listitem>
        <para>‘*’匹配零个或多个字符，从指定单词的当前位置开始到它结束的部分。例如搜索条件<literal>‘run*’</literal>将会匹配单词<literal>‘run’</literal>、<literal>‘runs’</literal>和<literal>‘running’</literal>。</para>
      </listitem>
    </itemizedlist>

    <itemizedlist>
      <listitem>
        <para>‘?’匹配任何单个字符。例如，<literal>‘run?’ </literal>会匹配单词<literal>‘runs’</literal>和<literal>‘running’</literal>单词中的<literal>‘runn’</literal>。</para>
      </listitem>
    </itemizedlist>

    <para>匹配将显示为蓝色粗体。需要注意‘*’和‘?’在正则表达式中有特殊含义，所以这里所说的通配符搜索只适用于精确搜索和关键字搜索（见下文）。</para>
  </section>

  <section id="search.methoids">
    <title>搜索方式和选项<indexterm class="singular">
        <primary>搜索</primary> <secondary>方式和选项</secondary>
      </indexterm></title>

    <para>使用单选按钮选择搜索方法。下面是可用的搜索方式：</para>

    <variablelist>
      <varlistentry>
        <term>精确搜索</term>

        <listitem>
          <para>找出包括指定字符串的片段。精确搜索查找某条短语，也就是说，如果输入了多个词汇，只有它们以相同顺序出现时才会被找出来。搜索 <literal>open file</literal> 将找到所有 <emphasis><literal>open file</literal></emphasis> 字符串出现的地方，但不会找到 <emphasis><literal>file opened</literal></emphasis> 或 <emphasis><literal>open input file</literal></emphasis>.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term>关键字搜索</term>

        <listitem>
          <para>找出所有包括指定关键字的片段，不考虑顺序。选择关键字搜索以搜索任何数目、任何顺序出现的所有词汇。OmegaT 会显示所有词汇都出现了的片段清单。关键字搜索和互联网 Google 搜索引擎的 “使用所有词”方式相似（AND 逻辑）。使用关键字<emphasis><literal>open file</literal></emphasis>进行搜索将找到所有<emphasis><literal>open file</literal>、</emphasis><emphasis><literal>file opened</literal>、<literal>open input file</literal>以及<literal>file may not be safe to open</literal></emphasis>等字符串出现的地方。</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><emphasis role="bold">正则表达式</emphasis></term>

        <listitem>
          <para>搜索字符串将会被当作正则表达式进行处理。例如在上文示例中的搜索字符串[a-zA-Z]+[öäüqwß]在目标片段中寻找包含疑似来自德语键盘的字符的单词。正则表达式是一种功能强大的字符串实例查找方法。请到<link linkend="chapter.regexp">正则表达式</link>章节了解更多信息。</para>
        </listitem>
      </varlistentry>
    </variablelist>

    <para>对上面的几种方式，您还可以选择下列选项：</para>

    <itemizedlist>
      <listitem>
        <para><emphasis role="bold">区分大小写</emphasis>：搜索工作将会严格匹配指定字符串，即遵循大小写状态。</para>
      </listitem>

      <listitem>
        <para><emphasis role="bold">Space matches nbsp</emphasis>: when this
        option is checked, a space character put in search entry can match
        either a normal space character or a non-breacking space (\u00A)
        character.</para>
      </listitem>

      <listitem>
        <para><emphasis role="bold">源片段：</emphasis>在源片段中搜索</para>
      </listitem>

      <listitem>
        <para><emphasis role="bold">目标片段：</emphasis>在目标片段中搜索</para>
      </listitem>

      <listitem>
        <para><emphasis role="bold">笔记：</emphasis>在笔记中搜索</para>
      </listitem>

      <listitem>
        <para><emphasis role="bold">in comments: </emphasis>search in comments
        to segments</para>
      </listitem>

      <listitem>
        <para><emphasis role="bold">Translated or untranslated:
        </emphasis>search in both translated and untranslated segments.</para>
      </listitem>

      <listitem>
        <para><emphasis role="bold">Translated: </emphasis>search only in
        translated segments.</para>
      </listitem>

      <listitem>
        <para><emphasis role="bold">Untranslated: </emphasis>search only in
        untranslated segments.</para>
      </listitem>

      <listitem>
        <para><emphasis role="bold">Display: all matching segments:
        </emphasis>if checked, all the segments are displayed one by one, even
        if they are present several times in the same document or in different
        documents.</para>
      </listitem>

      <listitem>
        <para><emphasis role="bold">Display: file names: </emphasis>if
        checked, the name of the file where each segment is found is displayed
        above each result.</para>
      </listitem>

      <listitem>
        <para><emphasis role="bold">Search in Project</emphasis>: check
        <emphasis>Memory</emphasis> to include the project memory
        (project_save.tmx file) in the search. Check <emphasis>TMs</emphasis>
        to include the translation memories located in the
        <filename>tm</filename> folder in the search. Check
        <emphasis>Glossaries</emphasis> to include the glossaries located in
        the <filename>glossary</filename> folder in the search.</para>
      </listitem>

      <listitem>
        <para><emphasis role="bold">Search in Files: </emphasis>search in a single file or a folder containing a set of files. When searching through files (as opposed to translation memories), OmegaT restricts the search to files in source file formats. 因此，尽管 OmegaT 能充分处理 <filename> tmx</filename> 文件，但它不会在进行文件搜索时把它们纳入搜索范围。</para>
      </listitem>
    </itemizedlist>

    <para>If you click on the button <guibutton>Advanced options</guibutton>
    additional criteria (author or changer of the translation, date
    translated, excluding orphan segments, etc) can be selected. When
    <emphasis>Full/Half width char insensitive</emphasis> option is checked,
    searches for fullwidth forms (CJK characters) will match halfwidth forms
    and vice versa.</para>
  </section>

  <section id="search.result.display">
    <title>搜索结果展示</title>

    <para>在搜索字段输入一个字符串然后点击搜索按钮，会把项目中所有包含输入字符串的片段显示出来。由于 <application>OmegaT</application> 将相同片段视为同一实体，因此将只显示第一个唯一的片段。片段以在项目中出现的顺序显示。已译片段会以上面显示原文、下面显示译文的形式显示，而未译片段只显示原始文本。</para>

    <para>Double-clicking on a segment opens it in the Editor for modifications (one single click does it when <emphasis role="bold">Auto-sync with Editor</emphasis> option is checked). 然后您可以转回搜索窗口以编辑找到的下一个片段，例如进行检查或者在必要时调整术语。</para>

    <para>In the Search window, you can use standard shortcuts (<keycombo>
        <keycap>Ctrl</keycap>

        <keycap>N</keycap>
      </keycombo>, <keycombo>
        <keycap>Ctrl</keycap>

        <keycap>P</keycap>
      </keycombo>) to move from one segment to another.</para>

    <para>可以同时打开多个搜索窗口。通过标题您可以快速了解它们的搜索内容：标题中包含了搜索用词。</para>
  </section>

  <section id="search.filter">
    <title>Filter entries in editor according to search</title>

    <para>For easier navigation in the search result set, you can apply the
    search to the editor. Press the <emphasis role="bold">Filter</emphasis>
    button on the bottom to limit the shown entries in the editor window to
    those that match the current search. You can use normal navigation to go
    to e.g. the next (untranslated) segment that matches the search
    criteria.</para>

    <para>NB:</para>

    <itemizedlist>
      <listitem>
         A search may be limited to 1000 items, so if you search on a common phrase, the editor then shows only those 1000 matching entries, and not all entries that match the search criteria. 
      </listitem>

      <listitem>
         A file might have no matching entries, so it will show empty. 
      </listitem>

      <listitem>
         If a search removes duplicates, those duplicates will not be in the Editor. 
      </listitem>
    </itemizedlist>

    <para>To remove a filter, press the <emphasis role="bold">Remove
    filter</emphasis> button, or reload a project.</para>
  </section>
</chapter>